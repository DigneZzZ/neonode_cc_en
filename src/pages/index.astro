import { DEFAULT_LANG } from "../src/consts"; // убедитесь, что путь корректен

export async function get() {
  return new Response(null, {
    status: 302,
    headers: {
      Location: `/${DEFAULT_LANG}/`,
    },
  });
}
